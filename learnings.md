- The "PascalCase" Convention: In React, we always start component names with a Capital Letter . So, instead of `function blogPage()` , you should use `function BlogPage()` .

- Tailwind uses a "Mobile First" approach. This means you define how things look on a phone first , and then use "prefixes" to change them for larger screens.

- 